<div id="appBusca9" class="small">
	<div style="overflow-y: auto;height:300px">
		<h4>Movimientos</h4>
		<table class="table table-condensed table-bordered table-stripped">
			<thead>
			<tr>
				<th>Fecha</th>
				<th>Tipo de Movimiento</th>
				<th>Número</th>
				<th>Peso</th>
				<th>Usuario</th>
			</tr>
			</thead>
			<tbody>
			<tr v-for="m in datos.movimientos">
				<td v-html="m.fecha_ingreso"></td>
				<td style="text-transform: uppercase;" v-html="m.tipo_movimiento"></td>
				<td>
					<div v-if="m.tipo_movimiento == 'ingreso'">
						<a :href="'{{ root }}/devolucionFormula/editar?id=' + m.id_modulo" target="_blank">
							<span v-html="m.numero"></span>
						</a>
					</div>
					<div v-if="m.tipo_movimiento == 'egreso'">
						<a :href="'{{ root }}/despachoProduccion/editar?id=' + m.id_modulo" target="_blank">
							<span v-html="m.numero"></span>
						</a>
					</div>
				</td>
				<td v-html="m.peso"></td>
				<td style="text-transform: uppercase;" v-html="m.username"></td>
			</tr>
			</tbody>
		</table>
	</div>
</div>

{{ script('js/lodash.js') }}
<script>

	var callbackOrden = null;

	var appBusca9 = new Vue({
		el: '#appBusca9',
		data: {
			datos: {{ datos|raw }},
		},
		methods: {
		},
		mounted: function () {
			// var self = this;
			// $("#txtbusca").autocomplete({
			// 	minLength: 2,
			// 	source: function (request, response) {
			// 		self.buscar();
			// 	}
			// });
		}
	})
</script>
